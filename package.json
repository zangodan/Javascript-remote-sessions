{
  "name": "moveo-home-assignment",
  "version": "1.0.0",
  "description": "",
  "main": "/Server/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd \"Server\" && npm install && node app.js",
    "build": "cd \"Client\" && npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react-scripts": "^5.0.1"
  }
}
